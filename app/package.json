{
  "name": "itsm-approuter",
  "version": "1.0.0",
  "description": "Application router: serves the SAPUI5 UI and proxies OData to the CAP service",
  "private": true,
  "engines": {
    "node": "^20 || ^22"
  },
  "dependencies": {
    "@sap/approuter": "^20"
  },
  "scripts": {
    "start": "node node_modules/@sap/approuter/approuter.js"
  }
}
